{
  "name": "podman-client",
  "version": "5.8.0",
  "description": "TypeScript bindings for the Podman RESTful API (libpod) — powered by Bun",
  "keywords": [
    "bun",
    "containers",
    "docker",
    "libpod",
    "podman"
  ],
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.0"
  }
}
